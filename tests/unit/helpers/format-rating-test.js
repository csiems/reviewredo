import { formatRating } from 'review-rockstar/helpers/format-rating';
import { module, test } from 'qunit';

module('Unit | Helper | format rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatRating([42]);
  assert.ok(result);
});
