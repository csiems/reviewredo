import { averageThis } from 'review-rockstar/helpers/average-this';
import { module, test } from 'qunit';

module('Unit | Helper | average this');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageThis([42]);
  assert.ok(result);
});
