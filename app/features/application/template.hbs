
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div>{{#link-to 'index' class='navbar-brand'}}<span class="glyphicon glyphicon-star-empty"></span> | Rockstar Reviews!{{/link-to}}</div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        {{#unless session.isAuthenticated}}
          <li><a href="#" {{action 'login' }}>Login</a></li>
        {{else}}
          <li>{{#link-to 'new-album'}}Add An Album{{/link-to}}</li>
          <li><a href="#" {{action 'logout' }}>Logout</a></li>
          <li>
            <a href='http://twitter.com/{{session.currentUser.username}}' target='_blank'>
              <img src='{{session.currentUser.profileImageURL}}' height='30px' width='30px'/>
            </a>
          </li>
        {{/unless}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="container">
  {{outlet}}
</div>
